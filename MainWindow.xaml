<Window x:Class="YearProgress.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Year Progress"
        Width="350" Height="36"
        ResizeMode="NoResize"
        Background="Transparent"
        AllowsTransparency="True"
        WindowStyle="None"
        WindowStartupLocation="Manual"
        Loaded="Window_Loaded"
        Closing="Window_Closing"
        Topmost="True"
        ShowInTaskbar="False"
        MouseLeftButtonDown="Window_MouseLeftButtonDown">

    <Window.Resources>
        <Style x:Key="Win10RoundedProgressBar" TargetType="ProgressBar">
            <Setter Property="Height" Value="8"/>
            <Setter Property="Foreground" Value="#00C853"/>
            <Setter Property="Background" Value="#2A2A2A"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ProgressBar">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="4"
                                SnapsToDevicePixels="True">
                            <Grid ClipToBounds="True">
                                <Rectangle x:Name="PART_Track" Fill="Transparent"/>
                                <Rectangle x:Name="PART_Indicator"
                                           Fill="{TemplateBinding Foreground}"
                                           RadiusX="4" RadiusY="4"
                                           RenderTransformOrigin="0,0.5"
                                           HorizontalAlignment="Left"/>
                            </Grid>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border CornerRadius="8"
            Background="#000000"
            BorderBrush="#2A2A2A"
            BorderThickness="1"
            Padding="6">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="2"/>   
                <ColumnDefinition Width="Auto"/>  
                <ColumnDefinition Width="4"/>   
                <ColumnDefinition Width="*"/>  
            </Grid.ColumnDefinitions>

            <TextBlock x:Name="TitleText"
                    Grid.Column="0"
                    FontFamily="Segoe UI"
                    FontSize="12"
                    FontWeight="Bold"
                    Foreground="#FFFFFF"
                    VerticalAlignment="Center"
                    Text="Year Progress: "/>

            <TextBlock x:Name="PercentText"
                    Grid.Column="2"
                    FontFamily="Segoe UI"
                    FontSize="12"
                    FontWeight="SemiBold"
                    Foreground="#FFFFFF"
                    VerticalAlignment="Center"
                    Text="0.00%"/>

            <ProgressBar x:Name="YearProgressBarControl"
                        Grid.Column="4"
                        Minimum="0"
                        Maximum="100"
                        Style="{StaticResource Win10RoundedProgressBar}"
                        VerticalAlignment="Center"/>
            
        </Grid>
    </Border>
</Window>
